// STUB — replaced by the V2 supplements build agent. Contract: renders null
// while the supplement checklist is disabled in settings.
export function SupplementsCard(_props: { todayKey: string }) {
  return null
}
